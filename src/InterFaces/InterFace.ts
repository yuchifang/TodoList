export type drinkType = {
    id: string;
    itemName: string;
    options: string;
    buyer: string;
}

export type updateDrinkType = {
    itemName: string,
    options: string,
    buyer: string
}
